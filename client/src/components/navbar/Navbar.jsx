import React from "react";
import { NavbarContainer } from "./Navbar.styled";

const Navbar = () => {
    return (
        <NavbarContainer>
            <h4>Navbar</h4>
        </NavbarContainer>
    );
};

export default Navbar;
