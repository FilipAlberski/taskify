{
    "name": "taskify",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "concurrently \"cd client && yarn dev\" \"cd server && nodemon index.js\"",
        "yarninstallall": "cd client && yarn install && cd ../server && yarn install && cd .. && yarn install",
        "npminstallall": "cd client && npm install && cd ../server && npm install && cd .. && npm install"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "concurrently": "^7.6.0",
        "express": "^4.18.2",
        "http-proxy-middleware": "^2.0.6",
        "nodemon": "^2.0.21",
        "yarn": "^1.22.19"
    }
}
